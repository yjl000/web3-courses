#Learn Cheatcodes

1、vm.expectRevert() // 预期接下来的交易（下一行代码）会回滚
2、vm.prank() // 将msg.sender设置为下一次调用的指定地址
3、vm.startPrank() // 设置后续所有调用的msg.sender，直到调用stopPrank为止
4、vm.stopPrank() // 停止设置调用的msg.sender
5、makeAddr() // 创建一个新的用户地址
6、vm.deal() // 设置用户的ETH余额
7、hoax() // deal + prank